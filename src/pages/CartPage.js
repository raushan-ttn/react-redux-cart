import React from 'react';
import { HeaderPage, ProductCart } from '../components';

const CartPage = () => {
  return (
    <>
      <HeaderPage />
      <ProductCart />
    </>
  );
};

export default CartPage;
